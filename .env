# VulnTrack Pro Environment Configuration

# OpenVAS/GVM Configuration
# The socket path for GVM (Greenbone Vulnerability Manager)
GVM_SOCKET=/var/run/gvmd/gvmd.sock

# OpenVAS credentials (get these from: sudo gvm-cli --gmp-username admin --gmp-password admin socket --socketpath /var/run/gvmd/gvmd.sock --xml "<get_version/>")
OPENVAS_USERNAME=admin
OPENVAS_PASSWORD=admin

# Scanning Configuration
MAX_CONCURRENT_SCANS=3

# Security
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Database (optional - defaults to SQLite)
# DATABASE_URL=sqlite:///vulntrack.db
